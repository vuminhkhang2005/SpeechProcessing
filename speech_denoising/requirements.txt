# Speech Denoising Project - Requirements
# Python 3.8+ recommended

# Deep Learning Framework
# NOTE: torch, torchvision, and torchaudio versions must be compatible.
# See https://pytorch.org/get-started/previous-versions/ for version compatibility.
torch==2.9.0
torchvision==0.24.0
torchaudio==2.9.0

# Audio Processing
librosa>=0.10.0
soundfile>=0.12.1
scipy>=1.10.0

# Data Processing
numpy>=1.24.0
pandas>=2.0.0

# Metrics
# NOTE: pesq is optional because it requires C compilation.
# On Windows, this requires Microsoft Visual C++ Build Tools.
# If pesq fails to install, the project will still work but PESQ metrics will be unavailable.
# Install separately with: pip install pesq
# pesq>=0.0.4
pystoi>=0.3.3

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
tensorboard>=2.13.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
hydra-core>=1.3.0

# For downloading dataset
requests>=2.31.0

# ===============================
# GUI Application Dependencies
# ===============================

# Audio playback for GUI
# NOTE: sounddevice requires PortAudio to be installed on your system
# Linux: sudo apt-get install libportaudio2
# macOS: brew install portaudio
# Windows: Usually works out of the box
sounddevice>=0.4.6

# tkinter is included with Python, but you may need to install it separately:
# Linux (Ubuntu/Debian): sudo apt-get install python3-tk
# Linux (Fedora): sudo dnf install python3-tkinter
# macOS: Usually included with Python from python.org
# Windows: Included with standard Python installation
